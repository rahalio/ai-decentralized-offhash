/**
 * GetPdScanResult handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: scans
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getPdScanResult } from "@offhash/api-server/domains/scans/handlers";
import type { ScansDDDDependencies } from "@offhash/api-server/domains/scans";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@offhash/tests/shared";
import { SCANS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetPdScanResult", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ScansDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ScansDDDDependencies>(
      SCANS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetPdScanResult, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { scanId: entity.id };

    await getPdScanResult(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetPdScanResult.execute).toHaveBeenCalledWith(expect.objectContaining({ scanId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { scanId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetPdScanResult, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getPdScanResult(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
