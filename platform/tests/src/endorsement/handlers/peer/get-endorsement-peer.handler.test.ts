/**
 * GetEndorsementPeer handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: endorsement
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getEndorsementPeer } from "@offhash/api-server/domains/endorsement/handlers";
import type { EndorsementDDDDependencies } from "@offhash/api-server/domains/endorsement";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@offhash/tests/shared";
import { ENDORSEMENT_USE_CASES } from "../__factories__/use-cases.js";

describe("GetEndorsementPeer", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: EndorsementDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<EndorsementDDDDependencies>(
      ENDORSEMENT_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetEndorsementPeer, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { peerId: entity.id };

    await getEndorsementPeer(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetEndorsementPeer.execute).toHaveBeenCalledWith(expect.objectContaining({ peerId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { peerId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetEndorsementPeer, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getEndorsementPeer(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
