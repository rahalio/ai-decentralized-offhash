/**
 * Configcerts DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: configcerts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Configcerts Domain Module - Composition root
 *
 * DDD: Composition root for configcerts domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  CompleteRepositoryAdapter,
  ConfigCertReviewRepositoryAdapter,
} from "@offhash/adapters/configcerts";
import { getIdGeneratorService } from "@offhash/adapters";
import type { AdapterDynamoDBClient } from "@offhash/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCompleteConfigCertReview,
  ExecuteCreateConfigCertReview,
  ExecuteGetConfigCertReview,
  ExecuteListConfigCertReviews,
} from "@offhash/services/configcerts/usecases";
// Ports (interfaces)
import type {
  CompleteRepository,
  ConfigCertReviewRepository,
} from "@offhash/services/configcerts/ports";

export interface ConfigcertsDomainModule {
  repos: {
    completes: CompleteRepository;
    configCertReviews: ConfigCertReviewRepository;
  };
  useCases: {
    completes: {
      create: ExecuteCompleteConfigCertReview;
    };
    configCertReviews: {
      create: ExecuteCreateConfigCertReview;
      get: ExecuteGetConfigCertReview;
      list: ExecuteListConfigCertReviews;
    };
  };
}

export function buildConfigcertsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ConfigcertsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      completes?: CompleteRepository;
      configCertReviews?: ConfigCertReviewRepository;
    } = {};

    obj.completes = new CompleteRepositoryAdapter(dynamoClient);
    obj.configCertReviews = new ConfigCertReviewRepositoryAdapter(dynamoClient);
    return {
      completes: obj.completes!,
      configCertReviews: obj.configCertReviews!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    completes: {
      create: new ExecuteCompleteConfigCertReview(executionContext, idGenerator, repos.completes),
    },
    configCertReviews: {
      create: new ExecuteCreateConfigCertReview(executionContext, idGenerator, repos.configCertReviews),
      get: new ExecuteGetConfigCertReview(executionContext, idGenerator, repos.configCertReviews),
      list: new ExecuteListConfigCertReviews(executionContext, idGenerator, repos.configCertReviews),
    },
  };
  return { repos, useCases: useCases as ConfigcertsDomainModule["useCases"] };
}
