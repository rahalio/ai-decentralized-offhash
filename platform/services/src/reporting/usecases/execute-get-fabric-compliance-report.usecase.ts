/**
 * ExecuteGetFabricComplianceReport - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: reporting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetFabricComplianceReport Use Case
 *
 * DDD: Application use case for get Compliance.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetFabricComplianceReportInput, GetFabricComplianceReportOutput } from "../dto/compliance.dto";
import type { ExecutionContextService, IdGeneratorService } from "@offhash/services/_shared/index.js";
import type { ComplianceRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteGetFabricComplianceReport {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly compliance: ComplianceRepository
  ) {}

  async execute(input: GetFabricComplianceReportInput): Promise<GetFabricComplianceReportOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.rptId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.compliance.getFabricComplianceReport({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as GetFabricComplianceReportOutput;
  }
}
