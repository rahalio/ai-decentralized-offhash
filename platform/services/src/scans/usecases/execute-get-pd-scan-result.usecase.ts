/**
 * ExecuteGetPdScanResult - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: scans
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetPdScanResult Use Case
 *
 * DDD: Application use case for get Scan.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetPdScanResultInput, GetPdScanResultOutput } from "../dto/scan.dto";
import type { ExecutionContextService, IdGeneratorService } from "@offhash/services/_shared/index.js";
import type { ScanRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetPdScanResult {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly scan: ScanRepository
  ) {}

  async execute(input: GetPdScanResultInput): Promise<GetPdScanResultOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.scnId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).scanId) {
      throw new ValidationError("scanId is required");
    }
    const scanId = (input as any).scanId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.scan.getPdScanResult({ scanId: scanId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${scanId}`);
    }

    return entity as GetPdScanResultOutput;
  }
}
