/**
 * ExecuteGetErasureDrill - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: erasures
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetErasureDrill Use Case
 *
 * DDD: Application use case for get ErasureDrill.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetErasureDrillInput, GetErasureDrillOutput } from "../dto/erasure-drill.dto";
import type { ExecutionContextService, IdGeneratorService } from "@offhash/services/_shared/index.js";
import type { ErasureDrillRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetErasureDrill {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly erasureDrill: ErasureDrillRepository
  ) {}

  async execute(input: GetErasureDrillInput): Promise<GetErasureDrillOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ersId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).drillId) {
      throw new ValidationError("drillId is required");
    }
    const drillId = (input as any).drillId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.erasureDrill.getErasureDrill({ drillId: drillId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${drillId}`);
    }

    return entity as GetErasureDrillOutput;
  }
}
