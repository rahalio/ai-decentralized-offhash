/**
 * ExecuteGetConfigCertReview - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: configcerts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetConfigCertReview Use Case
 *
 * DDD: Application use case for get ConfigCertReview.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetConfigCertReviewInput, GetConfigCertReviewOutput } from "../dto/config-cert-review.dto";
import type { ExecutionContextService, IdGeneratorService } from "@offhash/services/_shared/index.js";
import type { ConfigCertReviewRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetConfigCertReview {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly configCertReview: ConfigCertReviewRepository
  ) {}

  async execute(input: GetConfigCertReviewInput): Promise<GetConfigCertReviewOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cfgId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).reviewId) {
      throw new ValidationError("reviewId is required");
    }
    const reviewId = (input as any).reviewId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.configCertReview.getConfigCertReview({ reviewId: reviewId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${reviewId}`);
    }

    return entity as GetConfigCertReviewOutput;
  }
}
