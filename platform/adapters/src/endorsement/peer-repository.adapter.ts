/**
 * PeerRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: endorsement
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * PeerRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements PeerRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  PeerRepository,
} from "@offhash/services/endorsement";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { PeerRepositoryDdb } from "./peer-repository.ddb.js";

/**
 * PeerRepositoryAdapter
 *
 * Implements PeerRepository port for infrastructure layer.
 */
export class PeerRepositoryAdapter implements PeerRepository {
  private readonly ddb: PeerRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new PeerRepositoryDdb(this.dynamoClient);
  }

  async listEndorsementPeers(input: Parameters<PeerRepository['listEndorsementPeers']>[0]): Promise<Awaited<ReturnType<PeerRepository['listEndorsementPeers']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listEndorsementPeers(input);
  }

  async getEndorsementPeer(input: Parameters<PeerRepository['getEndorsementPeer']>[0]): Promise<Awaited<ReturnType<PeerRepository['getEndorsementPeer']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getEndorsementPeer(input);
  }
}
